global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'eventhub-monitor'

scrape_configs:
  - job_name: 'backend'
    static_configs:
      - targets: ['backend:3000']
        labels:
          service: 'eventhub-backend'
    metrics_path: '/metrics'
